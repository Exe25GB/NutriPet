spring:
  datasource:
    driver-class-name: oracle.jdbc.OracleDriver
    url: jdbc:oracle:thin:@df1_high?TNS_ADMIN=./Wallet_DF1
    username: NutriPetBD_04
    password: 7_9tmvh78EMmR
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true

logging:
  level:
    org.hibernate: DEBUG
    com.zaxxer.hikari: DEBUG
    java.sql: DEBUG

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /doc/swagger-ui.html

server:
  port: 8084


eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    webclient:
      enabled: true  #Le dice a Eureka que no busque Jersey
  instance:
    instance-id: ${spring.application.name}:${random.value}